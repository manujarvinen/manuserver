# The finished ISO lands in the repo root, where it is easy to find and easy to
# drag onto a USB stick. It is about a gigabyte.
/manuserver-*.iso

# The build's scratch space, which is much larger and much less interesting.
files/iso/build/

# The throwaway Postgres cluster `manuserver.sh site_dev` builds for itself.
files/dev/.cluster/
